#ack-grep, a grep-like tool for developer

# no grouping, use same output as grep
--noheading

# only search with case sensitivity if there is mixed case
--smart-case

# Sort the files
--sort-files

# ignore folder
--ignore-dir=build

# ignore tags file
--ignore-file=ext:tag
--ignore-file=ext:tags
--ignore-file=is:tags

# change color of matched pattern
--color-match=bright_red
--color-filename=cyan
# same color as grep --color-filename=magenta
--color-lineno=green
